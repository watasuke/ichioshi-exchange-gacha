<!doctype html>
<html lang="ja">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>新規投稿</title>
  </head>
  <body>
    <div class="container">
        <%= form_tag("/posts/create") do %>
            <div class="form-group">
                <% @post.errors.full_messages.each do |message| %>
                    <div class="form-error">
                        <%= message %>
                    </div>
                <% end %>
            <div class="col-auto">
                <label for="title">イチオシするもの</label>
                <input type="text" class="form-control border border-secondary" name="title" placeholder="なにをイチオシするか入力してください">
            </div>
            </div>
            <div class="form-group">
            <div class="col-auto">
                <label for="comment">コメント</label>
                <textarea class="form-control border border-secondary" name="comment" placeholder="どんなところがイチオシなのか、想いをつづってください"><%= @post.comment %></textarea>
            </div>
            </div>
            <button type="submit" class="btn btn-primary btn-lg mx-auto d-block">イチオシを投稿する</button>
        <% end %>
    </div>
  </body>
</html>